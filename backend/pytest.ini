[pytest]
testpaths = tests
addopts = -q
markers =
    smoke: uchdan-uchga oqim testlari (docker compose up -d kerak: Postgres/MinIO/Redis)
    unit: tashqi servissiz, faqat test bazasiga tayanadigan testlar
