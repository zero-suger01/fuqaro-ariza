FROM python:3.13-slim

# ffmpeg — audio transkript uchun (STT pipeline), libmagic1 — python-magic
# fayl validatsiyasi uchun (docs/11-devops.md §1: macOS'da shu ikkisi brew
# bilan o'rnatiladi, shu yerda apt bilan).
RUN apt-get update && apt-get install -y --no-install-recommends \
        ffmpeg \
        libmagic1 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN useradd --create-home --shell /bin/bash appuser && chown -R appuser:appuser /app
USER appuser

EXPOSE 8000

# Worker xuddi shu image'dan, faqat docker-compose.prod.yml'da command
# almashtiriladi: `arq app.worker.WorkerSettings` (docs/11 D2).
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
